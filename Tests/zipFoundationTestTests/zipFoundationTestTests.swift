import XCTest
@testable import zipFoundationTest

final class zipFoundationTestTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(zipFoundationTest().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
